/* ================================================================
   EXPORTACIÓN DE MODELOS - MONGODB
   ================================================================ */

const ChatLog = require('./ChatLog');
const ActivityLog = require('./ActivityLog');

module.exports = {
    ChatLog,
    ActivityLog
};